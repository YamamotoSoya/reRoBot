<!-- claude: docs/report テンプレート。debug-report スキルから生成。-->
# <問題のタイトル — 「<コンポーネント> の <症状>」形式を推奨>

- 日付: <YYYY-MM-DD>
- 環境: <ホスト OS / コンテナ名 / ROS distro / 主要パッケージ等>
- 対象ブランチ: `<branch>` (HEAD: `<commit hash> <commit subject>`)
- 関連ファイル:
  - `<path/to/file1>`
  - `<path/to/file2>`

## TL;DR

<原因と対処を 2〜4 行で要約。読み手はここだけで全貌を把握できること。
具体的なパラメータ名 / ファイル名 / 数値を含めること。>

## 症状

<再現条件 / 観測されたログ / 期待動作との差分。
「いつ」「どこで」「何が起きるか」「正常時はどうあるべきか」の 4 点を書く。>

```
<再現コマンドや観測されたログ抜粋 (時刻・ノード名込み)>
```

## 切り分けの記録

<仮説と検証を時系列で残す。当たらなかった仮説も書く (将来同じ罠を踏まないため)。>

### 1. <仮説 / 観測の見出し>

<どんな仮説でどう検証したかを 1 段落 + データで示す。>

| 項目 | 観測値 | 備考 |
|------|--------|------|
| <metric> | <value> | <note> |

### 2. <次の仮説 / 観測>

<...>

### 3. <最終的に当たった仮説>

<...>

## 根本原因

<なぜそれが起きるかを、観測値・仕様・タイミング図などで論証する。
ここを書けない時点では修正に進まない。
可能なら「なぜ今まで顕在化しなかったか」も書く (構成変更との関係)。>

## 修正

<実際の差分 / 設定変更 / コマンド。ファイルパスを明記する。
ビルド・再起動が必要かどうかも書く。>

```<lang>
<diff or code>
```

### なぜこの値 / この方法か

- <選択肢を選んだ根拠 (定量的に書けるなら定量で)>
- <他の選択肢を採らなかった理由>
- <この値が破綻する条件 (将来再評価のトリガー)>

## 検証

| 項目 | 修正前 | 修正後 |
|------|--------|--------|
| <観測量1> | <値> | <値> |
| <観測量2> | <値> | <値> |

検証手順:

```bash
<再現性のあるコマンド列 (他の人が同じ確認をできること)>
```

## 教訓 / 今後の予防

1. <ルール化できる学び — 「今後 X するときは Y を確認する」形式で書く>
2. <将来同じ罠を避けるためのチェックリスト項目>
3. <関連する未解決課題 / フォロアップ TODO (あれば)>
