<!-- claude: feature-doc スキルの設計文書。Claude 作成。-->

# <機能名 (日本語可)>

- 日付: <YYYY-MM-DD>
- 対象パッケージ: <`パッケージ名` (パス。submodule なら明記)>
- 対象 ROS: <ROS 2 Jazzy(reRoBot コンテナ `rerobot_env`)等>
- 関連文書: <docs/issue・docs/report への相対リンク。無ければ「該当なし」>

## 1. 目的・概要

<何のための機能か。1〜3 段落>

実装した機能:
- <機能 1>

スコープ外 (意図的に未対応):
- <やらなかったこと + 理由>

## 2. 設計の勘所

### 2.1 <最重要の設計判断>

<なぜこの方式か。実測・制約は「実測: …」、判断は「判断: …」と書き分ける>

却下した代替案:
- <代替案>: <却下理由>

### 2.2 <次の設計判断>

<...>

## 3. データフロー

```
<トピック・サービス・ノードの ASCII 図。名前と型は実物から転記>
```

## 4. 使い方

```bash
<起動・操作コマンド。実際に動かしたものをそのまま>
```

<パラメータ・キー操作等の表が必要なら | 項目 | 値 | 説明 | 形式で>

## 5. 変更ファイル一覧

- `<パス>` — <変更概要>

## 6. 既知の制限

- <制限・未検証事項。無ければ「該当なし」>
